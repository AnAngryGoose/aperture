# ── Build outputs ───────────────────────────────────────────────────────────
/bin/
/web/build/
/web/.svelte-kit/
/web/node_modules/
/dist/

# Go test + coverage artifacts
*.test
*.out
coverage.txt
coverage.html

# ── Runtime state ───────────────────────────────────────────────────────────
# Local SQLite database (and its WAL / SHM journal companions)
*.db
*.db-shm
*.db-wal
*.db-journal

# Logs
*.log
logs/

# ── Local config / secrets ──────────────────────────────────────────────────
# Catches .env, .env.local, .env.production, etc. anywhere in the tree.
.env
.env.*
!.env.example

# ── Editor / OS noise ───────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.iml
*.swp
*.swo
*.swn
*~
.*.swp

# ── Working / scratch files ─────────────────────────────────────────────────
# Annotated bug log used during reviews — local working doc, not shipped.
ISSUES.md
*.bak
*.orig
*.tmp
scratch/
tmp/
docs